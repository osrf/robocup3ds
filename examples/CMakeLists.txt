cmake_minimum_required(VERSION 2.8)

include_directories(${ROBOCUP3DS_INCLUDE_DIRS})
link_directories(${ROBOCUP3DS_LIBRARY_DIRS})

# add_executable(example example.cc)
# target_link_libraries(example library)
